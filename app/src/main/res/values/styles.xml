<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CourseTitle" parent="@android:style/TextAppearance.Material.Large" tools:targetApi="lollipop">
        <item name="android:textColor">@color/plural_sight_orange</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CourseText" parent ="@android:style/TextAppearance.Material.Large" tools:targetApi="lollipop">
        <item name="android:textColor">@color/text_title</item>
        <item name="android:textSize">@dimen/text_title</item>
    </style>
    
    <style name="AddFab" parent="@style/Widget.Design.FloatingActionButton">
        <item name="srcCompat">@drawable/ic_baseline_add_circle_24</item>
        <item name="android:tint">@android:color/white</item>
        <item name="backgroundTint">@color/plural_sight_orange</item>
    </style>
</resources>